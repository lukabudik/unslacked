[project]
name = "unslacked-datagen"
version = "0.1.0"
description = "Free-running persona simulation that generates a realistic enterprise Slack dataset into Neon"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "psycopg[binary]>=3.2.0",
    "python-dotenv>=1.0.0",
]

[tool.datagen]
# defaults documented in README; overridable via generate.py CLI flags
