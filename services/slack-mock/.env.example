# Neon Postgres connection string (pooled). Get it from the Neon dashboard.
# Without this set, the slack-mock falls back to in-memory fixture data so the
# app and API still run out-of-the-box.
DATABASE_URL=postgresql://user:password@ep-xxx.eu-central-1.aws.neon.tech/unslacked?sslmode=require
