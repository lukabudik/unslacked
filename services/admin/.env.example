# When backend is live: set USE_MOCK=false and point at the Railway backend URL.
NEXT_PUBLIC_USE_MOCK=true
NEXT_PUBLIC_BACKEND_URL=

# Duvo provisioning (server-side only — never expose to client)
DUVO_API_KEY=
DUVO_TEAM_ID=
